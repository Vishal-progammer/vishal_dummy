defmodule HrmWeb.GuestController do
  use HrmWeb, :controller

  # def index(conn, _params) do
  #   guests =Hrm.Grs.all_guests()
  #   conn =assign(conn, :guests, guests)
  #   render(conn, "index.html")
  # end



end
